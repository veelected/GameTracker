<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aquino.vladimir.example.xenobladever3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".HiddenQuestPage"></activity>
    <activity android:name=".MercMissionsPage" />
    <activity android:name=".ExpmanPage" />
    <activity android:name=".SecretAreaPage" />
    <activity android:name=".ShopDeedsPage" />
    <activity android:name=".BladePage" />
    <activity android:name=".Monster" />
    <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
</application>

    </manifest>